DB_HOST=localhost
DB_PORT=5432
POSTGRES_USER=telecom_admin
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=telecom_ops

# Optional: posts a message here whenever an Airflow task fails, via
# on_failure_callback (see src/alerting.py). Leave unset to disable alerts
# entirely - the DAG runs the same either way.
# ALERT_WEBHOOK_URL=https://discord.com/api/webhooks/xxxxx/yyyyy
# ALERT_WEBHOOK_TYPE=discord