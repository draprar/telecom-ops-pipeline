---
description: Dyscyplina inżynierska — zakres, testy, wzorce ETL tego repo
alwaysApply: true
---

Komunikuj się z użytkownikiem po polsku. Pracuj jak senior utrzymujący ten kod, nie jak jednorazowe zadanie.

## Zakres i decyzje

- Rób to, o co poproszono. Problemy poza zakresem zgłoś na końcu, nie zmieniaj ich po cichu.
- Nie zmieniaj ustaleń z README **"Design decisions & known simplifications"** bez decyzji użytkownika.
- Przy sprzeczności README vs reguła Cursora vs kod: **README i kod wygrywają**. Zaktualizuj regułę albo zapytaj — nie cofaj kodu do starszej checklisty.
- Trade-off albo dwie rozsądne drogi architektury → zapytaj, nie zgaduj.

## Wzorce tego projektu

- Etapy: extract → validate → quarantine → transform → load. Nowa odpowiedzialność = nowa funkcja/moduł, nie wciskaj jej w obcy etap.
- DAG: `PostgresHook` / Airflow Connection. Skrypt standalone: `os.getenv()` / `load.py`. Nie mieszaj ścieżek.
- Alerting: łap błędy ścieżki webhooka (kontekst, `requests`, JSON), loguj bez URL, nie re-raise. Goły `except Exception` nie jest celem.
- Kwarantanna = błąd wiersza. Raise = awaria systemowa (brak pliku, pad bazy).

## Testy i jakość

- Nowa/zmieniona logika w `src/`, `dags/`, `scripts/` dostaje test w `tests/` w tej samej zmianie. Test sprawdza zachowanie, nie tylko brak wyjątku.
- Test pada → napraw kod, nie osłabiaj asercji. Nie wyciszaj lintera/`except` bez usunięcia przyczyny.
- Nie hardkoduj pod jeden test ani pod aktualne dane syntetyczne.
- Flaky → diagnoza, nie większy timeout.
- Martwy kod w edytowanej funkcji można usunąć od razu. Żadnych łatek, zakomentowanego kodu, `TODO: później`.

## Pętla

`zrozumienie → implementacja → test → poprawka → review diff`

Nie kończ na „pierwsze uruchomienie działa”. Przed oddaniem: wymaganie spełnione, testy i lint przeszły, diff bez śmieci. Git: patrz `git-handoff.mdc`.
